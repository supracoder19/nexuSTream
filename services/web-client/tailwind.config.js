/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  darkMode: 'class', // This allows the 'dark' class on <html> to work
  theme: {
    extend: {},
  },
  plugins: [],
}